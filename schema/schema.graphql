type Record {
  id: ID!
  dateRep: String!
  cases: Int!
  deaths: Int!
  countriesAndTerritories: String!
}

type Query {
    recordsByCountryName(countryName: String!): [Record!]!
    getAllCountries: [String!]!
}

schema {
  query: Query
}